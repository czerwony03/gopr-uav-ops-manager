name: Build Android Signed APK & AAB
permissions:
  contents: read
  pull-requests: write
concurrency:
  group: build-android
  cancel-in-progress: false

on:
  workflow_dispatch:
    inputs:
      buildType:
        description: "Choose build type"
        required: true
        default: "apk"
        type: choice
        options:
          - apk
          - aab
          - both
      environment:
        description: "Choose environment"
        required: true
        default: "test"
        type: choice
        options:
          - test
          - prod
  workflow_run:
    workflows: [ "Automatic Version Bump" ]
    types:
      - completed
  issue_comment:
    types: [created]

jobs:
  build-android:
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success') ||
      (github.event_name == 'issue_comment' &&
       (github.event.comment.body == '/build' || github.event.comment.body == '/build android') &&
       github.event.issue.pull_request != null)
    runs-on: self-hosted
    environment: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.environment || 'test' }}

    defaults:
      run:
        working-directory: goprUavOpsManager

    env:
      VARS_CONTEXT: ${{ toJson(vars) }}
      BUILD_TYPE: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.buildType || (github.event_name == 'issue_comment' && (github.event.comment.body == '/build' || github.event.comment.body == '/build android') && 'apk') || 'both' }}
      EXPO_PUBLIC_GIT_COMMIT_HASH: ${{ github.sha }}
      SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
      ENVIRONMENT: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.environment || 'test' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Patch app.json for test environment
        if: env.ENVIRONMENT == 'test'
        run: |
          echo "🔧 Patching app.json package name for TEST..."
          sed -i 's/dev.redmed.gopruavopsmanager/dev.redmed.gopruavopsmanagertest/g' app.json
          sed -i 's/icon.png/icon-test.png/g' app.json
          sed -i 's/GOPR UAV Ops Manager/GOPR UAV Ops Manager Test/g' app.json

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: goprUavOpsManager/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Set env vars from vars context JSON
        run: |
          echo "$VARS_CONTEXT" | jq -r 'to_entries | .[] | "\(.key)=\(.value)"' >> $GITHUB_ENV
        shell: bash

      - name: Set Git commit hash
        run: echo "EXPO_PUBLIC_GIT_COMMIT_HASH=${GITHUB_SHA}" >> $GITHUB_ENV

      - name: Set Eas build commit hash
        run: echo "EAS_BUILD_GIT_COMMIT_HASH=${GITHUB_SHA}" >> $GITHUB_ENV

      - name: Export Android SDK paths (self-hosted only)
        if: runner.environment == 'self-hosted'
        run: |
          echo "ANDROID_HOME=$HOME/Android" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=$HOME/Android" >> $GITHUB_ENV
          echo "$HOME/Android/cmdline-tools/latest/bin" >> $GITHUB_PATH
          echo "$HOME/Android/platform-tools" >> $GITHUB_PATH

      - name: Restore google-services.json
        run: |
          echo "$GOOGLE_SERVICES_JSON" | base64 --decode > google-services.json
          cat google-services.json
        shell: bash

      - name: Prebuild
        run: npx expo prebuild --platform android
        env:
          EXPO_PUBLIC_FIREBASE_API_KEY: ${{ secrets.EXPO_PUBLIC_FIREBASE_API_KEY }}

      - name: Patch Gradle for release signing
        run: |
          FILE=android/app/build.gradle
          echo "🔧 Checking $FILE for release signing config..."
          if ! grep -q "signingConfigs.release" "$FILE"; then
            echo "Adding release signing config..."
            sed -i '/signingConfigs {/a \
                    release {\n\
                        storeFile file(project.findProperty("MYAPP_UPLOAD_STORE_FILE"))\n\
                        storePassword project.findProperty("MYAPP_UPLOAD_STORE_PASSWORD")\n\
                        keyAlias project.findProperty("MYAPP_UPLOAD_KEY_ALIAS")\n\
                        keyPassword project.findProperty("MYAPP_UPLOAD_KEY_PASSWORD")\n\
                    }' $FILE
            sed -i 's/signingConfig signingConfigs.debug/signingConfig signingConfigs.release/' $FILE
          else
            echo "✅ Release signing config already present."
          fi
        shell: bash

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      #- name: Setup Gradle cache
      #  uses: actions/cache@v4
      #  with:
      #    path: |
      #      goprUavOpsManager/android/.gradle/caches
      #      goprUavOpsManager/android/.gradle/wrapper
      #    key: ${{ runner.os }}-gradle-${{ env.ENVIRONMENT }}
      #    restore-keys: |
      #      ${{ runner.os }}-gradle-${{ env.ENVIRONMENT }}

      - name: Decode Keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > android/app/my-release-key.keystore
        shell: bash

      - name: Verify keystore
        run: |
          ls -lh android/app/*.keystore
          keytool -list -v -keystore android/app/my-release-key.keystore -storepass ${{ secrets.ANDROID_KEYSTORE_PASSWORD }} || true
        shell: bash

      - name: Verify environment
        run: |
          printenv
        shell: bash

      - name: Build Release APK
        if: env.BUILD_TYPE == 'apk' || env.BUILD_TYPE == 'both'
        run: |
          cd android
          ./gradlew assembleRelease \
            -PMYAPP_UPLOAD_STORE_FILE=my-release-key.keystore \
            -PMYAPP_UPLOAD_KEY_ALIAS=${{ secrets.ANDROID_KEY_ALIAS }} \
            -PMYAPP_UPLOAD_STORE_PASSWORD=${{ secrets.ANDROID_KEYSTORE_PASSWORD }} \
            -PMYAPP_UPLOAD_KEY_PASSWORD=${{ secrets.ANDROID_KEY_PASSWORD }}
        env:
          EXPO_PUBLIC_FIREBASE_API_KEY: ${{ secrets.EXPO_PUBLIC_FIREBASE_API_KEY }}
        shell: bash

      - name: Build Release AAB
        if: env.BUILD_TYPE == 'aab' || env.BUILD_TYPE == 'both'
        run: |
          cd android
          ./gradlew bundleRelease \
            -PMYAPP_UPLOAD_STORE_FILE=my-release-key.keystore \
            -PMYAPP_UPLOAD_KEY_ALIAS=${{ secrets.ANDROID_KEY_ALIAS }} \
            -PMYAPP_UPLOAD_STORE_PASSWORD=${{ secrets.ANDROID_KEYSTORE_PASSWORD }} \
            -PMYAPP_UPLOAD_KEY_PASSWORD=${{ secrets.ANDROID_KEY_PASSWORD }}
        env:
          EXPO_PUBLIC_FIREBASE_API_KEY: ${{ secrets.EXPO_PUBLIC_FIREBASE_API_KEY }}
        shell: bash

      - name: Signing report
        if: env.BUILD_TYPE == 'apk' || env.BUILD_TYPE == 'both'
        run: |
          cd android
          ./gradlew signingReport \
            -PMYAPP_UPLOAD_STORE_FILE=my-release-key.keystore \
            -PMYAPP_UPLOAD_KEY_ALIAS=${{ secrets.ANDROID_KEY_ALIAS }} \
            -PMYAPP_UPLOAD_STORE_PASSWORD=${{ secrets.ANDROID_KEYSTORE_PASSWORD }} \
            -PMYAPP_UPLOAD_KEY_PASSWORD=${{ secrets.ANDROID_KEY_PASSWORD }}
        env:
          EXPO_PUBLIC_FIREBASE_API_KEY: ${{ secrets.EXPO_PUBLIC_FIREBASE_API_KEY }}
        shell: bash

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: android-build-apk-${{ env.ENVIRONMENT }}
          path: |
            goprUavOpsManager/android/app/build/outputs/apk/release/*.apk
          if-no-files-found: ignore

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: android-build-aab-${{ env.ENVIRONMENT }}
          path: |
            goprUavOpsManager/android/app/build/outputs/bundle/release/*.aab
          if-no-files-found: ignore
